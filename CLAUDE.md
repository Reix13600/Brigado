# Brigado — Project Context for Claude Code

Staff hours, roster, and payroll SaaS for French restaurants (HCR sector). Built with Firebase (Hosting, Firestore, Auth, Cloud Functions), React + TypeScript + Vite. Stripe for billing, Resend for transactional email.

## Stack & structure

* `src/` — React app. `App.tsx` handles routing (slug-based: `/{restaurantSlug}` = the app, `/` = public Landing page, `/mentions-legales` `/cgv` `/confidentialite` `/contact` `/features` = static marketing pages).
* `src/components/ManagerDashboard.tsx` and `StaffDashboard.tsx` — the two main views. ManagerDashboard is very large (~5000 lines); most features live here.
* `functions/` — separate Node project (own `package.json`/`tsconfig.json`), Cloud Functions v2. Deploy separately from hosting: `firebase deploy --only functions` vs `firebase deploy --only hosting`.
* Firestore structure: `restaurants/{slug}` (config, staff array, revenueByWeek, ownerContact, managerEmails) with subcollections `entries`, `advances`, `scheduledShifts`, `activeClockIns`, `announcements`, `messages`, `timeOffRequests`, `swapRequests`. Separate `managers/{uid} -> {restaurantId, email}` lookup collection (supports multiple managers per restaurant, all equal access). Also top-level `platformAdmins/{email}` and `bonusCodes/{code}` — see "Site Manager admin dashboard" below — both fully closed to client access.
* Production domain: `brigado.solutions` (Firebase Hosting custom domain). Firebase project: `brigado-a33b1`.
* Reserved slugs: `src/utils/reservedSlugs.ts` (client, fail-fast UX) and `functions/src/reservedSlugs.ts` (server, the real boundary — mirrored by hand, `functions/` cannot import from `src/`). Covers every `STATIC_PAGES` key in App.tsx plus `admin` plus some infrastructure-shaped names reserved for later. Enforced in `RegisterPage.tsx` (before Stripe checkout) and inside `provisionRestaurant` itself (the slug arrives via Stripe's `client_reference_id` and never has to pass through the form, so the server check is the one that actually matters) and in `redeemBonusCode`.

## Business context

* Pricing: EUR 39/month or EUR 390/year, 7-day free trial, card required upfront. Stripe Payment Links (not custom Checkout code) with `trial_period_days` set per-link.
* Registration flow: RegisterPage collects restaurant name/slug/email/contact name/phone (required)/city (required, added Aug 2026)/postcode (optional) -> Stripe Payment Link (all fields ride in `client_reference_id`, pipe-delimited, along with the page's FR/EN toggle state) -> webhook (`stripeWebhook` in functions) provisions the restaurant, manager account, and sends a password-setup email via Resend. Provisioning stores `signupEmail` (the durable "main registered contact" — survives later manager add/remove), `preferredLang`, and `ownerContact: { contactName, phone, postcode, city }` on the tenant doc.
* Target market: French restaurants/small hotels. French is the default language everywhere (`lang` state defaults to `"fr"`); English is a toggle, not the primary language.
* Competitor context: Combo/Snapshift charges per-employee (~EUR 5-7/employee/mo); Brigado's flat fee is a deliberate differentiator for restaurants with bigger teams.

## Key architectural decisions (don't relitigate without reason)

* Trust model: staff use anonymous Firebase Auth (no real accounts, identity enforced by in-app PIN only) — this was a deliberate simplicity choice. Managers use real email/password or Google auth.
* No hard-deletes of staff data by default. Archiving a staff member (`active: false`) hides them from active rosters but never touches their historical entries/payroll — this is intentional, for French payroll record retention (~5 years). Permanent deletion is only exposed in the UI once 5+ years have passed since their last logged activity (see the Former Staff popup in ManagerDashboard).
* Compliance engine (`src/utils/compliance.ts`) cites real Code du travail articles + the HCR collective agreement. Every rule is individually toggleable (not all-or-nothing), defaults to only the 4 near-universal rules being on. There's an explicit "what this does NOT track" disclaimer in the UI — never remove that; overstating legal coverage is a real liability, not just a copy nitpick.
* Print-safe portals: any printable document (QR poster, Timesheet, Bookkeeper export) renders via `createPortal` to `document.body` with class `print-portal-root`. The print CSS in `src/index.css` hides everything except that portal using `body:has(.print-portal-root) > *:not(.print-portal-root) { display: none }`. Do NOT put `print:hidden` on the portal's own root element — that was a real bug once (hides the print area along with everything else). Do NOT remove the `:has()` guard — without it, printing anything else in the app (Roster Planner, Entries) goes blank.
* Messages auto-delete after 30 days (`cleanupOldMessages` scheduled function) — private chat only, NOT announcements, NOT entries/payroll (those are kept indefinitely, no retention automation built yet beyond the 5-year staff-deletion gate above).
* Bilingual pattern: most newer components use inline `lang === "fr" ? "..." : "..."` rather than the `translations.ts` dictionary (which covers older/core UI). Both patterns coexist; match whichever pattern the file you're editing already uses.

## Scheduling/payroll series — PHASE A: tolerance-aware effective hours (2026-08-15)

**This is Phase A of a multi-phase build.** Later phases — the **variance view** (built in Phase B, below), **weekly hours counter**, **shift-template tray**, **overtime warning**, **no-show alerts**, and the **payroll-ready screen** — all consume `src/utils/effectiveHours.ts` rather than reimplementing the maths. That module exists *before* any of those UI features so there is exactly one definition of "effective hours" in the codebase. If you're reading this wondering why a calculation exists with no UI on top of it: that's why, it's deliberate.

### ⚠️ NOT wired into the live payroll export — on purpose

The existing payroll CSV/bookkeeper export still sums raw `HourEntry.hours` exactly as it always has. **Nothing about current payroll numbers changed in Phase A.** The tolerance calculation must be validated against real data in Phase B's variance view (which shows effective vs actual side by side) *before* it is allowed to move real pay figures. Do not "finish the job" by wiring `effectiveHours` into `triggerExportPayrollCSV` without that validation step.

### The tolerance rule (easy to get backwards — implement exactly)

`toleranceMinutes` is a per-restaurant grace window. At each edge of a shift:

* **Clock-IN**, when `|actual − scheduled| <= tolerance` → `effectiveStart` = the **EARLIER** of the two. Outside tolerance → **actual, raw**.
* **Clock-OUT**, when `|actual − scheduled| <= tolerance` → `effectiveEnd` = the **LATER** of the two. Outside tolerance → **actual, raw**.

Within tolerance it is generous to the employee at both edges; outside tolerance there is no adjustment in either direction. Worked examples (scheduled 09:00–17:00, tolerance 10 min), all covered by tests:

| Punch | Within tolerance? | Effective |
|---|---|---|
| in 08:55 (early) | yes | **08:55** (earlier of the two) |
| in 09:04 (late) | yes | **09:00** (earlier of the two) |
| in 09:15 (late) | **no** | **09:15** raw |
| out 17:04 (late) | yes | **17:04** (later of the two) |
| out 16:56 (early) | yes | **17:00** (later of the two) |
| out 16:40 (early) | **no** | **16:40** raw |

Boundary is inclusive (`<=`): exactly ±10 min counts as *within*. A tolerance of `0` is meaningful and preserved (no adjustment ever) — only negative/NaN falls back to the default.

### Raw clock data is NEVER mutated

Every function in `effectiveHours.ts` is **pure and computed at read/export time**. It never writes to Firestore and never mutates its arguments. `HourEntry.shifts[].startTime/endTime` — written by `clockOut()` in `api.ts` — is the legal record of what actually happened and stays byte-identical; "effective hours" is a *derived* number for pay/reporting only. A later phase needs the raw actual time to show true deviations, so this is load-bearing, not stylistic. Tests assert it explicitly (repeat calls identical, deep-frozen inputs accepted, `entry.hours` unchanged after computation).

### Where things live

* **`toleranceMinutes`** → `GeneralConfig.tolerance_minutes` (optional). Default **10** (`DEFAULT_TOLERANCE_MINUTES`, mirrored in `api.ts`'s `DEFAULT_CONFIG`). Edited in **Settings → "Hours & tax"**, directly under the weekly OT limit — hours-related settings stay in one card. Clamped 0–60 on save. The field carries a visible FR/EN note that it is not yet applied to payroll, so a manager changing it doesn't expect their numbers to move.
* **Overtime threshold reuses the existing source of truth** — `getContractHours()` mirrors `ManagerDashboard`'s function exactly: `member?.contract || config.overtime_limit` (which itself defaults to 35). **No new 35h constant was introduced.** Note the `||` (not `??`) is *deliberately* duplicated: a `0` contract falls through to the config value, matching current live behaviour. Changing it to `??` would silently alter existing overtime numbers.

### Two things Step 0 established about the data model (verified 2026-08-15, not assumed)

1. **There is NO stored link between a scheduled shift and its clock record.** `HourEntry` has no `scheduledShiftId`; `ScheduledShift.id` is a `string` while `HourEntry.id` is a `number`. Pairing is *derived*: same `name` + `date`, then greedy **nearest-start-time** matching, each scheduled shift consumed at most once. This handles split shifts (09:00–14:00 + 18:00–23:00) regardless of clock order, and leaves surplus clock records unscheduled.
2. **The payroll export never consulted the schedule at all** — it sums `HourEntry.hours`, which `clockOut()` derives purely from actual clock-in→clock-out. So Phase A did not have an existing matcher to align with; it built the first one.

**Unscheduled shifts use raw actual times** (no schedule ⇒ no adjustment is even definable), and are flagged `unscheduled: true` for Phase B. A consequence worth knowing: a restaurant that never uses scheduling gets effective totals **identical** to its current payroll.

### Known gap this phase surfaced — no manager audit trail on hour edits

~~`correctionNote`/`correctionAt` are written **only by `StaffDashboard`**...~~ **FIXED in Phase B** — see that section below. `saveInlineEdit()` now stamps `editedBy`/`editedAt`/`previousHours`/`editReason`.

### Tests

**`npm test`** (`vitest run`) — **62 test cases / 107 assertions, all passing** as of 2026-08-17 (44/65 from this section, 18/42 from Phase B's `variance.test.ts` — see below). `npm run test:watch` for the watch mode. Covers all six worked examples in both directions, inclusive boundaries, tolerance 0, overnight/cross-midnight shifts, split-shift pairing, immutability, non-worked entry types, contract-threshold precedence, and malformed-data degradation.

**This is the project's first and only test runner.** Vitest was chosen because it reuses the existing `vite.config.ts` — no separate build/transform config to keep in sync — and because more phases in this series will add more tests. It runs pure-Node (no jsdom): `effectiveHours.ts` is deliberately framework-free so it can be exercised without rendering anything. If a later phase needs to test a React component, that's when to add `jsdom`/`@testing-library`, not before.

*(Historical note: Phase A originally shipped these as a dependency-free `tsx` script under `npm run test:hours`, to avoid adding a runner for one module. Migrated to vitest immediately afterwards, before Phase B, on the reasoning that it's cheaper to add now than after several phases of tests exist. `tsx` remains a devDependency — the `scripts/brevo-*.ts` tooling still uses it.)*

The test file lives in `src/` but is imported by nothing, so it is **not** bundled (verified against `dist/`).

## Scheduling/payroll series — PHASE B: variance view + approval workflow (2026-08-17)

Second phase of the series introduced in Phase A above. Builds the first UI on top of `effectiveHours.ts`'s matching logic: a **Variance tab** in ManagerDashboard showing scheduled-vs-actual deltas per employee per month, with a manager approval workflow on top. Also closes the audit-trail gap Phase A found.

### Step 0 findings (established before writing any code)

* **New tab, not an extension of StatsPage.** `StatsPage.tsx` is a pure aggregate analytics view (recharts trends/donuts/gauges, no row-level actions). Variance is an operational approve/reject workflow at the individual-day granularity — a fundamentally different interaction shape. A new `"variance"` tab was added to `ManagerDashboard`'s tab list, gated on `enable_scheduling` exactly like the existing `"schedule"` tab (no schedule data ⇒ nothing to compare against). Its own component, `src/components/VarianceTab.tsx`, follows `StatsPage.tsx`'s existing convention of a standalone tab component taking `{ appData, lang, theme, onRefresh }` and calling the API layer directly, rather than growing `ManagerDashboard.tsx` (already ~5000 lines) further.
* **`effectiveHours.ts` already exposed what Phase B needed — no refactor.** `pairShiftsToSchedule()` already returns the raw matched pairing (which clock record matched which scheduled shift, or `null`) *before* any tolerance rounding, and `EffectiveShift`/`EffectiveDay` already carry `actualStart`/`actualEnd`/`actualHours` (raw) alongside the tolerance-adjusted `effective*` fields. Phase B's `variance.ts` reuses `pairShiftsToSchedule()` directly and never touches the tolerance/effective-hours math at all — see "raw vs. effective hours" below for why that separation matters.
* **Manager identity**: `auth.currentUser` (imported from `../firebase`) is reliable inside `ManagerDashboard` and `api.ts` — `ManagerDashboard` gates its own content behind an internal Firebase Auth check (`watchAuthState` + `isAuthorizedManager`, lines ~148–158), so by the time any manager-only code runs, `auth.currentUser` is a real, non-anonymous manager account, not a staff anonymous session. `approvedBy`/`editedBy` use `auth.currentUser?.email || auth.currentUser?.uid || "unknown"`.
* **Subcollection naming**: `varianceApprovals`, matching the project's existing camelCase-plural-noun convention (`entries`, `advances`, `scheduledShifts`, …).

### Raw hours vs. effective hours — two different numbers now exist, on purpose

Variance is computed from **raw actual minutes** (`Shift.hours` as clocked, via `pairShiftsToSchedule`'s output) — **never** Phase A's tolerance-adjusted `effectiveHours`. This is deliberate: variance exists to show a manager the true deviation between what was scheduled and what actually happened, so they can decide whether to approve it. Feeding it tolerance-adjusted numbers would hide small deviations *before* the manager ever saw them — exactly backwards from the point of a review workflow. `effectiveHours.ts` and `variance.ts` therefore read the same underlying clock/schedule data but answer different questions: one is "what should we pay" (tolerance-forgiving, still not wired into payroll — see Phase A), the other is "what actually happened vs. plan" (raw, review-oriented). Do not conflate them or "simplify" by having one call the other's rounded output.

### Variance data model — computed live, only the approval decision persisted

Same "pure function, no cached duplicate" discipline as `effectiveHours.ts`. `src/utils/variance.ts` exports:

* `computeDayVariance(entry, allScheduled)` — one `HourEntry`'s variance against that day's schedule. Returns `null` for non-`"worked"` entries, entries with no shifts, or when every matched pair fell within the 1-minute floor. Reuses `pairShiftsToSchedule` for matching; never duplicates it.
* `aggregateMonthlyVariance(name, entries, allScheduled, approvals)` — per-employee aggregation over whatever period of entries it's handed (same "caller does its own date-range bucketing" convention as `aggregateEffectiveHours`). Only `status === "approved"` entries participate, mirroring the live payroll export's own filter.

**Nothing about the variance NUMBERS is ever written to Firestore.** The only new persisted state is the human decision: `restaurants/{slug}/varianceApprovals/{date}__{encodeURIComponent(name)}` (`VarianceApproval` in `types.ts`) — `{ name, date, approvedBy, approvedAt, note? }`. Absence of a doc for a given (name, date) means "pending"; there is no separate pending doc. `api.ts` exposes `approveVarianceDay`, `approveAllRemainingVariance` (batch, caller supplies exactly the pending dates from `aggregateMonthlyVariance`), and `invalidateVarianceApproval`.

**The 1-minute floor is a calculation-wide inclusion filter, not just a display filter.** A component is only included in `computeDayVariance`'s output — and therefore counted in `aggregateMonthlyVariance`'s `scheduledHours`/`clockedHours` totals — when `|actualMinutes - scheduledMinutes| >= 1`. This is what guarantees `differenceHours === approvedHours + pendingHours` exactly, with no separate rounding path to drift out of sync. Unscheduled (extra) shifts have no floor applied — a real clock record with no matching schedule is never "noise," regardless of size.

**No-shows are excluded by construction, not by an extra check.** A scheduled shift with zero clock records that day has no `HourEntry` for `computeDayVariance` to even be called on — the caller (`aggregateMonthlyVariance`) iterates `entries`, not `scheduledShifts`, so a true no-show day simply never produces a `DayVariance`. Reserved for a later no-show-alerts phase, per the original spec — do not fold no-show detection into this module.

### Approval-invalidation-on-edit — an approval must never silently apply to changed numbers

`saveInlineEdit()` (ManagerDashboard) now calls `invalidateVarianceApproval(matched.name, matched.date)` **unconditionally** on every hours edit, immediately after `saveEntry()`. Deleting a doc that doesn't exist (the common case — most edited days were never approved) is a harmless no-op, so there's no need to check first. This is the mechanism that keeps an approval from silently continuing to apply after the underlying hours change: any edit to a day's hours reverts that day to pending, full stop, regardless of whether the new value happens to net out the same.

### Fixed: manager-edit audit trail (`saveInlineEdit`)

`HourEntry` gained `editedBy?`, `editedAt?`, `previousHours?`, `editReason?` (optional free text, entered via a small input that appears next to the hours field while editing). `saveInlineEdit()` now stamps all but the reason automatically and passes the reason through when the manager fills it in. **Purely additive** — the hours value itself is saved exactly as before; nothing about how hours are calculated changed. This is separate from `correctionNote`/`correctionAt`, which remain staff-initiated-correction-only.

### Firestore rules — `varianceApprovals` is manager-only for READ, unlike every other subcollection

```
match /varianceApprovals/{id} {
  allow read, write: if isManagerOf(restaurantId) && !tenantBlocked(restaurantId);
}
```

Every other subcollection (`entries`, `advances`, `scheduledShifts`, …) follows "any signed-in visitor can read, managers write" — deliberately not followed here, since staff have no reason to see whether a manager has signed off on a day's variance. Verified against the Firestore emulator (`scripts/test-variance-rules.mjs`, run via `firebase emulators:exec --only firestore "node scripts/test-variance-rules.mjs"` — not part of `npm test`, needs a live emulator and the `@firebase/rules-unit-testing` devDependency pinned to `^4` to match this project's `firebase@^11`, since `@firebase/rules-unit-testing@5` requires `firebase@^12`, which was NOT upgraded to as part of this phase): 13 checks — manager read/write allowed, anonymous-staff and unauthenticated denied, cross-tenant manager denied, blocked-tenant manager denied, and the invalidation delete (including the no-op-on-absent-doc case) all pass.

### Deliberately out of scope this phase

* **No-shows** — excluded by construction (see above); reserved for a later no-show-alerts phase.
* **The live payroll export (`triggerExportPayrollCSV`) was not touched.** Same discipline as Phase A: it still sums raw `HourEntry.hours` with zero schedule consultation. Verified by diff — no changes anywhere near that function.
* **`deleteStaffMemberData`** (the 5-year-retention permanent-erasure path) was not updated to also delete a departing staff member's `varianceApprovals` docs. Minor, known gap — orphaned approval records for a permanently-deleted staff member are not PII-sensitive (they're just a manager's own past sign-off), but worth closing if that function is touched again.

### Spot-checked against production `la-vague` data (2026-08-17)

Hand-verified (not clicked through the live UI — no manager credentials available in this session) against real Firestore documents: **Reigo, 2026-07-16** has a scheduled shift (09:00–15:30, 6.5h) but the clock record only covers 10:00–12:45 (2.75h) → correctly computes **−3h45**. **Reigo, 2026-07-17** has a clock record with no matching scheduled shift that day → correctly falls into "Unscheduled" at the full +5.5h. Anthony has clock records but zero `ScheduledShift` docs anywhere in the tenant → correctly all-Unscheduled. No `ScheduledShift` exists past 2026-08-09 in production, so the tab's default "current month" (August) view shows everything as Unscheduled right now — expected given the data, not a bug.

## Staff-facing "My Schedule" view (2026-08-18)

Staff previously had no dedicated way to see their own upcoming shifts. This section documents what existed before, what was added, and a real bug this work surfaced and fixed in Phase B's `fetchAppData`.

### Step 0 — what already existed (this was NOT a blank slate)

Two pieces of real, if partial, schedule visibility already existed in `StaffDashboard.tsx` before this change — worth knowing so neither gets duplicated by a future phase:

* **"My Weekly Rota"** (`config.enable_scheduling`-gated grid, current week only) — this doubles as the day-picker for the hours-entry form: tapping a *past* day selects it for logging; future days show their scheduled start/end/role but are disabled (can't log hours for a day that hasn't happened). It has no next-week view and was never meant to be a pure "what am I working" screen — logging hours is its primary job.
* **"My upcoming shifts"** — inside the swap-request ("Cover") tab of the 3-tab "MY SPACE" widget (Messages/Time off/Cover), a flat, unbounded (today onward, no week boundary) list already existed, used so a staff member can pick which of their own shifts to request cover for. Collapsed behind a toggle, and only reachable by opening the Cover tab.

Neither is a substitute for "check my schedule at a glance" — the first is fused into a different task (logging hours), the second is buried inside a different feature (swap requests) and has no week framing. The new **"Mon planning" / "My Schedule"** card is genuinely new UI, but its query logic is NOT new: it reuses the exact same `s.name === selectedStaff && ...date...` matching already established by both pieces above (see "Two things Step 0 established" in Phase A, above — `ScheduledShift` has no staff-ID link, only `name` + `date`; this reuses that fragile-but-established match exactly, no ID-based refactor attempted).

### What was added

* `getWeekDates()` (local to `StaffDashboard.tsx`) gained an optional `weekOffset: number = 0` param — `0` is this week (unchanged default, used everywhere it always was), `1` is next week (used only by the new card). Every existing call site is untouched.
* A new card, placed right after "Your hours this week" and before the MY SPACE widget — the first thing under the header, since "do I work today" is the single most useful fact on this screen. This-week/next-week toggle, today's row visually distinguished (lime ring + "Aujourd'hui"/"Today" label instead of the weekday name), role shown via the same `getRoleColor` + `t('role...')` pattern used elsewhere in this file, and a small amber "Cover requested" chip reusing `swapRequests` data (filtered to non-denied, unlike the Cover tab's own `myOwnSwapIds` which doesn't filter status — a stale badge on a denied request would be misleading in a read-only view, so this one deliberately differs).
* **`ScheduledShift` has no `overnight` boolean** (unlike `Shift`, which does — the two are different types; don't confuse them). Overnight is derived the same way `plannedHours.ts`'s `shiftDurationHours` and ManagerDashboard's `getShiftHours` already do: `endTime <= startTime`. Shown as a small amber "+1" suffix on the time range.
* Empty state — "Pas encore de service prévu." / "No shifts scheduled yet." — matches this file's own established empty-state voice exactly (`text-xs text-slate-500 italic`, same phrasing shape as "Pas encore de messages." elsewhere in this component).

### Real bug found and fixed while verifying this: Phase B broke `fetchAppData` for everyone

Testing this locally (dev server against the live `brigado-a33b1` backend — there is no separate dev project) surfaced `permission-denied` on **every** app load, staff and manager alike. Root cause: Phase B's `fetchAppData()` added `getAllVarianceApprovals()` to its `Promise.all` **unconditionally**, but `varianceApprovals` rules are manager-only for read (`isManagerOf(restaurantId)`). An anonymous staff session — and even a manager's session on the very first load, before they've completed `ManagerDashboard`'s own internal auth gate — is always denied that read. Because `Promise.all` rejects on any single failure, this took down the *entire* app load, not just the variance data: nobody could reach even the staff PIN screen or the manager login form.

**Fixed in `api.ts`**: `getAllVarianceApprovals()` now catches `permission-denied` specifically and returns `[]` — mirroring this codebase's existing "wrapped and logged, never thrown" philosophy (see the Brevo section). A denied read is the *expected* outcome for any non-manager session, not an error; only a manager session (where `isManagerOf()` passes) gets real data back, which is the only session that ever renders `VarianceTab` anyway. Any other error code still throws normally.

This was caught by testing against real data, not by review — worth remembering next time a new subcollection with restricted read access is added to `fetchAppData`'s `Promise.all`: an unconditional add there is only safe when every session type that calls `fetchAppData` can actually read it.

### Deliberate v1 scope — not oversights

* **Live view, not draft/publish.** Shows the schedule exactly as currently saved, the moment a manager saves a shift — no separate "published" state. Matches how "My Weekly Rota" and the Manager Rota Planner already work; introducing a publish step would be a bigger, separate feature.
* **Own shifts only.** No manager-side "who's working when" cross-staff view was requested or built here — out of scope.
* **No push notifications.** The view is pull-only (open the app, check). A staff member isn't notified when a new shift is scheduled for them. Reasonable v1 boundary, not a bug — flagged here as a plausible future phase, same as the no-show-alerts / overtime-warning items already noted in the scheduling series above.

## Scheduling/payroll series — PHASE D: shift-template tray (2026-08-19)

Fourth phase of the series (after Phase A's `effectiveHours.ts`, Phase B's variance view, Phase C's `plannedHours.ts` weekly counter — the latter built but, as of this phase, still undocumented here beyond its own code comments). A persistent tray under the Rota Planner's weekly grid where a manager saves reusable shift shapes (label + start + end + optional role) and drags one onto a grid cell to instantly create a shift for whoever it's dropped on.

### Step 0 findings

* **Drag-and-drop mechanism: native HTML5 D&D, reused exactly, not duplicated.** The grid's existing "move a shift" interaction (`handleDragStart`/`handleDropShift` in `ManagerDashboard.tsx`) is plain `draggable` + `e.dataTransfer` — no library. Templates use the SAME `onDragOver`/`onDrop` targets already wired into the weekly grid's cells; the only addition is a second `dataTransfer` MIME type (`application/x-shift-template`, vs. the existing move-drag's bare `"text/plain"`) so `handleDropShift` can tell "move this existing shift" apart from "create a new one from this template" without a parallel drag system. Drop targets only exist in the **weekly** view (the monthly calendar grid has none) — same pre-existing limitation the move-a-shift drag already had; not new.
* **Role is a real, existing concept**: `RoleType`, `StaffMember.role`, `ScheduledShift.role`. A template's `role` is optional — when unset, a dropped template inherits the target staff member's own role (mirroring how the existing move-drag already resolves role on reassignment), rather than forcing every template to hardcode one.
* **Subcollection + rules convention**: followed `varianceApprovals` exactly (Phase B) — `shiftTemplates`, manager-only for both read and write, since staff have no legitimate reason to see or touch a manager's saved shortcuts.

### Data model — `shiftTemplates/{id}`

`ShiftTemplate` (`types.ts`): `{ id, label, startTime, endTime, role?, createdAt }`. Manager-driven only — added via a simple label+start+end(+role) form in the tray, no frequency-based auto-suggestion in this phase. Expected to hold a handful of rows per restaurant, not hundreds; no pagination or search was built.

```
match /shiftTemplates/{id} {
  allow read, write: if isManagerOf(restaurantId) && !tenantBlocked(restaurantId);
}
```

Verified against the Firestore emulator (`scripts/test-shift-templates-rules.mjs`, same pattern as `scripts/test-variance-rules.mjs`): 12/12 — manager read/write/delete allowed, anonymous-staff and unauthenticated denied, cross-tenant manager denied, blocked-tenant manager denied.

**`fetchAppData`'s new `getAllShiftTemplates()` reuses the exact `permission-denied` → `[]` guard** already fixed for `getAllVarianceApprovals()` in the "My Schedule" section above — a manager-only-read subcollection added to `fetchAppData`'s `Promise.all` is only safe when every session type that calls `fetchAppData` can survive a denial on it, and that fix is now the established pattern for the *next* subcollection like this too, not just a one-off patch. Confirmed locally against the real backend (dev server, anonymous session): zero console errors, app loads normally.

### A template is never consumed — and a drop is a real ScheduledShift, created the same way any other one is

Dragging a template does **not** delete or modify the template doc. It reads `label`/`startTime`/`endTime`/`role` and calls `saveScheduledShift()` — the exact same `api.ts` function `handleSaveScheduleShift` (the manual "Add Shift" modal) and the existing move-a-shift drag both already call — with a freshly generated id (`shift-{timestamp}-{random}`, same scheme as manual creation) and `hours` computed via the same local `getShiftHours()` the rest of `ManagerDashboard.tsx` uses. The resulting `ScheduledShift` doc is indistinguishable in shape from a manually-typed one. This is why Phase C's `computePlannedWeekTotal` (the running counter) needed zero changes to pick up template-created shifts: it filters `appData.scheduledShifts` by `name`/`date` only, with no notion of "how a shift was created."

### Verified

* Emulator rules: 12/12 (above).
* Data-level, against **real production `la-vague`** documents (not a mock): created one disposable template, then created two disposable `ScheduledShift` docs in the exact shape `handleDropShift`'s template branch produces — one for Reigo, one for Anthony, different dates — and confirmed both are correct and independent, and the template doc is unchanged (unconsumed) throughout. Then ran the real `computePlannedWeekTotal()` from `plannedHours.ts` against that real data: both employees' counters came back correct (`3.5/30h` under threshold for Reigo, `3.5/35h` for Anthony) with zero special-casing needed — direct proof of the shared-code-path claim above. All three disposable docs deleted afterward.
* Typecheck + build: clean, both root and `functions/` (this phase touches no Cloud Functions code).
* **Not verified**: the actual drag gesture and the tray's visual rendering in the browser — the Schedule tab sits behind real manager email/password or Google auth, which wasn't available in this session (same constraint noted when "My Schedule" was verified). The React drag handlers themselves typecheck and are small/deterministic; the Firestore-level behavior they produce is what was verified directly above.

### Deliberately out of scope this phase

* No frequency-based "popular shifts" auto-suggestion — manager-driven only, per the original spec.
* No template editing (label/time/role) — delete and re-add is the only path for now.
* Monthly-view drag support was never in scope; templates share the weekly-only limitation the move-a-shift drag already had.

## Staff UX round — Parts 1-8 (2026-08-20)

Eight-part staff-facing UX pass, mostly `StaffDashboard.tsx`, with Parts 6 and 8 extending the scheduling/payroll series (Phase A/B/D above) rather than starting something new. Grouped here as one section since they landed together in one task, not because they're one feature.

### Part 1 — shorter PIN-entry landing

The "Qui êtes-vous ?" name grid used to stay rendered (never hidden) even after a worker was identified — so every screen, every time, opened with a list of names to scroll past before reaching your own section. It now renders `{!selectedStaff && (...)}` — gone entirely once someone's picked. The only way back is a small "Pas vous ? / Not you?" link next to the welcome header, which calls the existing `handleReset()` (already used by "submit another") — re-picking a different name re-triggers their PIN exactly as before, nothing about the security model changed.

### Part 2 — compact clocked-in indicator

A small pill ("Pointé depuis 12:43" / "Clocked in since 12:43"), not a banner, shown right under the welcome header the moment a worker who's already clocked in opens their page. Deliberately independent of `shiftType`/`selectedDate` — unlike the full clock-in/out card further down the page (which only renders for `shiftType === "worked"` + today), this reads `activeClockIns` directly, since it's a status readout of real-world state, not tied to whatever the hours-logging form currently has selected.

### Part 3 — "My Schedule" rebuilt as a calendar/agenda hybrid

Was a flat this-week/next-week list (Aug 18 build, documented above) — unwieldy once a manager schedules a month or two ahead, per the task. Rebuilt around one `monthShifts` dataset (all of `selectedStaff`'s shifts in the currently-viewed month, with prev/next/today month navigation replacing the old this-week/next-week toggle) driving two layouts, CSS-switched at the `sm:` (640px viewport) breakpoint — no JS resize listener, no duplicated data fetch.

**Step 0 finding — the whole staff app is globally `max-w-md` (448px).** `#staff-app`'s outer container has always been capped at 448px, at every viewport width — a deliberate pre-existing choice (this app runs on a worker's own phone, not a shared desktop; see "Trust model" above). This mattered for interpreting "calendar grid on larger screens": breaking just the My Schedule card out of that cap (e.g. a `position: relative; left: 50%` full-bleed trick) was considered and rejected — the percentage in that CSS trick resolves against the nearest containing block, which here is still inside `max-w-md`, not the viewport, so it wouldn't actually have worked without an ancestor rewrite I couldn't safely verify without a live desktop browser this session. Instead, the grid renders **within** the existing cap: 448px still gives a 7-column month grid ~55px per column, which is a completely ordinary mobile-calendar density (comparable to Apple/Google Calendar's own phone month view). The real, meaningful split is `sm:` — virtually every phone is under 640px viewport width in portrait, so real workers (per the task's own "most workers will use this on a phone" framing) always get the agenda list; the grid only activates on a tablet/desktop viewport, where 448px of card width is still comfortable.

Grid cells show a day number, up to 3 role-colored dots for that day's shifts, and reuse the existing holiday/weekend/today accent conventions (`getFrenchHoliday`, established weekend sky-accent). Tapping a day reveals its shift(s) in a detail panel below the grid — same "tap for detail" idea as Part 4's popup, just inline since the grid has room. The agenda view (below `sm:`) needs no tap-to-reveal step: `monthShifts` renders top-to-bottom directly, reusing the exact row markup the old this-week list used.

**Explicitly untouched, per the task's own instruction**: "My Weekly Rota" (StaffDashboard) stays exactly as it was — current-week-only, doubles as the hours-logging day-picker. The two are genuinely different tools (forward-looking read-only calendar vs. this-week logging surface) and nothing here merges them.

### Part 4 — "today popup" on My Weekly Rota

Tapping a day in "My Weekly Rota" (any past-or-today day, matching the grid's existing clickability) now also opens a small popup showing that day's worked hours and a running total from Monday of that week through the tapped day — both computed with the exact same `status !== "pending"` filter `getWeekHours()` already uses for the week-progress bar, so the popup's numbers can never disagree with the header above it. Purely additive: the existing "tap a day to select it for logging" behavior is unchanged, the popup is a second effect of the same click, not a replacement for it.

### Part 5 — shift-type icons refreshed to lucide-react

The only literal pictorial "shift type" icons anywhere in the app are StaffDashboard's own "Type de service" selector (worked/absent/sick/holiday) — grepped for equivalents in `ManagerDashboard.tsx` and found none; entries render as text-label badges there, never emoji, so Part 5's scope is this one selector. Replaced ✅🚫🤒🏖️ with `CheckCircle2`, `XCircle`, `Thermometer`, `Palmtree` — lucide-react, already the icon library this whole app uses everywhere else (`ManagerDashboard.tsx` alone imports ~20 lucide icons); no new dependency added.

### Part 6 — time-off awareness in the Rota Planner

**Step 0, confirmed against the real data model before writing any code:**
1. `TimeOffRequest` already carries everything needed — `staffName`, an inclusive `startDate`..`endDate` range, `status: "pending" | "approved" | "denied"`. No schema change.
2. `ScheduledShift` links to an employee by `name` + a single `date`, same fragile-but-established match every other scheduling feature already relies on (see Phase A's "Two things Step 0 established"). A shift "conflicts" with time off exactly when its `date` falls inside an approved request's inclusive range for the same `name`.

New pure helper, `src/utils/timeOffConflicts.ts`'s `findTimeOffConflict(name, date, requests, statuses = ["approved"])` — one function, asked from both directions the Rota Planner needs: is there a conflict BEFORE a shift is saved (block), and does an EXISTING shift now conflict because time off was approved afterward (the reverse case). 8 unit tests (`timeOffConflicts.test.ts`) cover inclusive boundaries, cross-employee isolation, single-day ranges, and the status-filter default.

**Grid behavior:**
* Pending time off on a date → a small 🌴 flag in the cell's corner (heads-up only, title shows the range) — never blocks anything.
* Approved time off + an existing shift on that date (the reverse case: approval landed after the shift was already scheduled) → the shift card itself gets a rose ring/border plus an `InfoTooltip` explaining the conflict, exactly the same visual language `hasOverlapConflict`/`hasComplianceWarning` already use on shift cards, so a manager reads it as "this needs attention" without learning a new visual pattern.
* Approved time off + no shift yet → the empty cell gets a faint rose tint and an "On leave" label, so a manager sees it before even attempting to drop a shift there.
* The pending time-off request list (Requests tab) also shows an inline warning listing any shift(s) already scheduled inside the requested range, so a manager sees the coming conflict **before** clicking Approve, not only afterward on the grid they may not be looking at.

**Block + two-step override — one chokepoint, not five.** Every UI path that can create/move a scheduled shift (manual add/edit via the modal, drag-drop move, drag-drop from the shift-template tray, the duplicate button, and the copy-previous-week bulk action) used to call `saveScheduledShift()` directly. They now all funnel through one new `trySaveScheduledShift()` in `ManagerDashboard.tsx`: it checks `findTimeOffConflict(..., ["approved"])` first, and if there's a hit, HOLDS the save and opens a confirmation modal (`timeOffOverridePrompt` state) instead of writing anything — the actual `saveScheduledShift()` call only happens either when there's no conflict, or when the manager explicitly clicks "Schedule anyway" in that modal. This was a deliberate single-chokepoint design specifically so the block can't quietly drift out of sync across five near-identical call sites the way handleSaveScheduleShift/handleDropShift/etc. had already started to duplicate logic in.

`handleCopyPreviousWeekSchedule` is the one exception to the modal flow — it's a bulk loop, so a per-shift blocking dialog doesn't fit. It instead **skips** (never overwrites) any shift that would land on approved time off and reports exactly what was skipped in the completion alert, rather than either silently dropping it or blocking the whole batch on one conflict.

**Verified**: the pure conflict-detection logic via 8/8 unit tests (above) plus a live check against real production `la-vague` schedule data (Reigo's August rota, fetched via the app itself — see Verification below) confirming the underlying `scheduledShifts`/`timeOffRequests` shapes match what `findTimeOffConflict` expects. **Not verified**: the actual manager-authenticated click-through — badges rendering on a real conflict, the override modal appearing and requiring the second click, the bulk-copy skip-and-report message — because the Rota Planner sits behind real manager email/password or Google auth, unavailable in this session. This is the same recurring, documented constraint as Phase D's shift-template tray and the "My Schedule" view before it, not a new gap.

### Part 7 — Demandes (the staff page) UX

("Demandes" turned out to refer to `StaffDashboard.tsx` itself — there's no separate requests route; grepped to confirm.)

* **Quick clock-in/out, reachable without scrolling.** Folded into Part 2's compact indicator: when `strict_clock_required` is on, a small pill button sits right under the welcome header — "Clock in" when not clocked in, or the clocked-in-since pill plus a small "Clock out" button when already clocked in. Reuses the existing `handleClockIn`/`handleClockOut` directly (no new API calls) and acts on "now" regardless of whatever date is selected in the hours-logging form below — clocking in/out is always a right-now action. The full clock-in/out card further down the page (with the note textarea and the "cancel this clock-in" mistake-undo option) is untouched — this is an additional fast path, not a replacement.
* **"Your recent submissions" is now collapsible, collapsed by default.** Chose collapsible over a week/month filter dropdown because this file already has an established, proven pattern for exactly this — "Besoin d'un jour plus ancien ?" and "Mes prochains services" both use the identical toggle-with-chevron shape, both default closed. A filter control would be new UI vocabulary nothing else in this staff-facing file uses, and is fussier to operate one-handed on a phone than a single tap — reusing the existing pattern also keeps the page shorter by default, which is the same direction as Part 1.

### Part 8 — auto-approve toggle for variance under 15 minutes

Reuses the tolerance-setting's own Settings location (Hours & tax card, right below `tolerance_minutes`) and its `Toggle` component, per the task's instruction to reuse that pattern. New `GeneralConfig.auto_approve_variance_enabled?: boolean`.

**The critical constraint, and how it's actually implemented**: no `VarianceApproval` doc is ever written for an auto-approved day. `src/utils/variance.ts`'s `aggregateMonthlyVariance()` gained a 5th param, `autoApproveEnabled` (default `false` — every pre-existing caller keeps today's exact behavior unchanged), and a new **computed-only** `VarianceStatus` value, `"auto-approved"`: a day with no real approval record and `|deltaMinutes| < 15` (the threshold is a fixed exported constant, `AUTO_APPROVE_THRESHOLD_MINUTES`, not manager-configurable — only the on/off switch is) is assigned that status purely in-memory, on every call. `day.approval` stays `null` for these days precisely because nobody actually approved them — a genuine manual approval always wins even when a day would also qualify for auto-approve (tested explicitly). Reversibility is a direct consequence of nothing being persisted: flip the toggle off, and the exact same days compute back to `"pending"` on the very next render — there is no data migration step because there was never any data.

`VarianceTab.tsx` renders `"auto-approved"` with its own color (sky, not lime) and its own label ("Auto-approved" / "Approuvé auto") plus its own caption ("Under 15 min — no manual review on record") — never sharing lime/"Approved by X" styling with a real approval, so the two are never visually indistinguishable, per the task's explicit constraint. The tab header also shows a small badge when the toggle is on, so a manager looking at auto-approved days understands why without needing to go check Settings. For counting purposes (the summary card's Approved/Pending totals, and — later — the not-yet-built payroll-ready screen's "unexplained deviations" count, which will read this same `VarianceStatus` when it exists), an auto-approved day's minutes fold into `approvedHours`, since it IS being treated as effectively approved; this is the one and only place in the codebase that decides that, so a future consumer of variance status has nothing to reimplement.

**Verified**: 6 new unit tests in `variance.test.ts` (24/24 passing in that file) covering: default-off backward compatibility, auto-approve firing under the threshold with `approval === null`, the boundary being exclusive (exactly 15 min does NOT auto-approve), over-threshold staying pending regardless of the toggle, a real manual approval always taking precedence, and reversibility (same underlying data, toggle flipped, status flips back with zero mutation). This is a pure-computation module, so vitest was judged sufficient — no manager-UI click-through was needed to prove this part's correctness the way Part 6's grid interactions did.

### Verification summary (all parts)

* **Typecheck + build**: clean, root and `functions/` (this round touches no Cloud Functions code — `functions/` was rebuilt only to confirm it, per the standing convention).
* **Tests**: 104/104 (`npm test`) — up from 90, the 14 new ones are `timeOffConflicts.test.ts` (8) and the Part 8 additions to `variance.test.ts` (6).
* **Real production data, via the app itself (not screenshots)**: dev server against live `la-vague` data (per the standing note that there's no separate dev project), staff PIN bypassed with a **temporary, local-only** `?__mockStaff=Name` query-param hook added to `StaffDashboard.tsx` for this one verification pass and **fully removed immediately after** (confirmed via grep — no trace left in the diff) — same technique already used earlier for the logo-carousel verification. Confirmed against Reigo's real August schedule: Part 1's "who are you" list genuinely gone once identified; Part 3's My Schedule agenda view correctly spans the whole month (not just this/next week) and correctly renders a split-shift day (two rows, same date) and a "Cover requested" badge from real `swapRequests` data; Part 7's collapsed-by-default "Your recent submissions (5)" header; the quick clock-in button.
* **Not verified — pixel screenshots.** The Browser pane failed to composite frames for the entire back half of this session (a client-display-state issue, not a code problem — confirmed by every other check above passing normally) — the exact same failure mode already logged in the dark-mode contrast fix section above. Text/DOM-level verification (above) substitutes for it but is not the same as a visual check; still owed once the pane is available again.
* **Not verified — Part 6's manager-authenticated grid interactions** (badge rendering, the override modal, bulk-copy skip reporting) — no manager credentials available this session, the same recurring, already-documented constraint as Phase D and the original "My Schedule" build.
* No disposable test data was created this session (no production writes were made — the earlier logo-carousel verification's disposable-write attempt had already established that direct production writes get blocked by this environment's permission classifier, so this round relied on read-only/mock-based verification throughout instead).

## Dark-mode muted-text contrast fix (2026-08-19)

`text-slate-500`/`text-slate-600`/`text-slate-700` — used ~250 times across the app and marketing site (timestamps, captions, hints, empty states, secondary labels, table borders, FAQ copy) — failed WCAG AA against every dark background this app actually uses. Not eyeballed; computed via the standard relative-luminance formula:

| | `bg-slate-950` | `bg-slate-900` | `bg-slate-800` |
|---|---|---|---|
| `text-slate-400` (untouched) | 7.87:1 ✅ | 6.96:1 ✅ | 5.71:1 ✅ |
| `text-slate-500` (was) | 4.24:1 ❌ | 3.75:1 ❌ | 3.07:1 ❌ |
| `text-slate-600` (was) | 2.66:1 ❌ | 2.36:1 ❌ | 1.93:1 ❌ |
| `text-slate-700` (was) | 1.95:1 ❌ | 1.72:1 ❌ | 1.41:1 ❌ |

AA requires 4.5:1 for normal text (3:1 for large/bold ≥18pt or ≥14pt-bold). `text-slate-400` already passed everywhere and was left alone — it's the "secondary" tier. The light theme (`.theme-light`, see below) was **already** fixed in an earlier pass and was independently re-verified as passing (6.9–17.9:1 across its own backgrounds) — not touched by this fix.

**Fix — one shared token, not scattered inline overrides.** `text-slate-500/600/700` are consolidated to a single corrected "muted" tone, `#829ab1` (Tailwind's old cool-gray-400), via a global CSS override in `src/index.css` (plain rules + `!important`, same proven technique the existing `.theme-light` block already uses to reliably beat Tailwind v4's layered utility output — not a new pattern). Three *separate* replacement shades were considered and rejected: there isn't enough contrast headroom between the AA floor and `text-slate-400` on the app's brightest background (`bg-slate-800`, 5.71:1) to fit three visually distinct AA-passing steps below it. Three different *failing* shades were never a real hierarchy to begin with, so collapsing them to one loses nothing. `#829ab1` clears AA with real margin everywhere it's used (**5.02:1 minimum**, on `bg-slate-800`) while staying consistently ~0.7–1:1 dimmer than `text-slate-400` on every background, preserving the secondary/muted visual step. Placeholder variants (`placeholder-slate-600`, `placeholder-slate-700`, `placeholder:text-slate-600`) got the identical fix.

**Deliberate exception — the two print portals stay on the original values.** `#timesheet-print-area` and `#qr-poster-print-area` (Timesheet.tsx / BookkeeperExport.tsx / QRPoster.tsx) render their printable area on a **white** background via `createPortal` straight to `document.body`, entirely outside the app's dark theme. The *original* `text-slate-500`/`text-slate-600` values already passed AA there (4.76:1 / 7.58:1 on white) — applying the new brighter dark-mode value would have made them *worse* (2.91:1, a new failure). Two ID-scoped rules in `index.css` restore the original hex values specifically inside those two containers, overriding the global fix by specificity. Don't remove these thinking they're dead code — they're the reason print output didn't regress.

**Disabled-state `disabled:opacity-XX` dimming was deliberately left alone.** It's a WCAG-exempt pattern (1.4.3 excludes inactive UI components) used consistently for buttons across the app — the point of a disabled control is to visually read as unavailable. Any *text* inside a disabled element still gets the token fix above; only the intentional opacity dimming on top is untouched.

**Verified**: typecheck + build clean (root; this phase touches no Cloud Functions code). Computed-style checks (not eyeballed) against the running dev server confirmed `getComputedStyle(el).color === "rgb(130, 154, 177)"` (`#829ab1`) on real elements across three surfaces: the marketing Landing page (23 elements), the real StaffDashboard "My Schedule" empty state and Rota grid (20 elements), and the manager login screen. Zero console errors (aside from an unrelated Vite HMR WebSocket reconnect issue in this sandboxed dev environment, not an app error). **Not verified**: pixel screenshots — the Browser pane failed to composite frames in this session (a client-display-state issue, confirmed unrelated to the code change by every other check above) — before/after screenshots are still owed once that's available.

## Landing page — "How it works" explainer video (2026-08-12)

* The static chalkboard illustration (`src/assets/how-it-works.webp`) under the hero was replaced by a 10s animated explainer. `src/components/HowItWorksVideo.tsx` owns the whole behaviour; `Landing.tsx` just renders `<HowItWorksVideo />` in the FR branch.
* **Assets** (all in `src/assets/`, imported so Vite hashes them): `how-it-works.mp4` (1.44 MB, H.264 High, 1280x720, 24 fps, **no audio track**, faststart), `how-it-works-poster.webp` (106 KB — the video's own frame 0, which is the complete three-step board), `how-it-works-end.webp` (48 KB — the logo/CTA end screen). Source files were kept out of the repo; re-exports should come from the originals in `~/Downloads` (`we_need_to_little_edit_previou.mp4`, `Gemini_Generated_Image_yrf2aayrf2aayrf2.png`).
* The end-screen artwork is natively **2.22:1**, not 16:9. It was **padded** to 1280x720 with its own corner colour (`#040716`) at export, deliberately not cropped — cropping to 16:9 cuts ~10% off each side, which removes the fork/knife and star doodles. Both assets therefore share identical geometry and swap inside one `aspect-video` box with no layout shift.
* Behaviour: muted + `playsInline` + `preload="metadata"`, autoplay **only** once an IntersectionObserver (threshold 0.35) sees the section, plays through **once**, then holds on the end screen. It deliberately does **not** loop back; a small replay button is provided. `prefers-reduced-motion` renders the poster instead and **never requests the .mp4 at all** (verified), with an opt-in play button.
* The end-screen CTA is a **real `<a href="/register">`** covering the whole surface — not baked-in video pixels — so it is keyboard-focusable and goes to the same destination as every other primary CTA. The visible lime button is anchored at 84% of box height and is an affordance, not the tap target. That percentage is load-bearing: it clears the artwork's baked tagline (which ends at 74.7% of height) while fully covering the redundant "Brigado.solutions" line (81–84%), at both mobile and desktop widths where the button is ~18% vs ~11% of the height. Re-measure if the end-screen artwork is ever re-exported.
* **KNOWN GAP — the video's on-screen text is French only; no English version exists.** This is *not* currently a visible bug, because the section was already language-branched before this change: `Landing.tsx` renders the video for `lang === "fr"` and an entirely separate icon/text three-step grid for English visitors. So English visitors never see the video. If an EN video is ever produced, the EN branch is the place to wire it in — do not assume the video is bilingual, and do not "fix" the EN branch by pointing it at this FR video.
* `src/assets/how-it-works.webp` is now **unused** but intentionally left on disk as a rollback path. Nothing imports it, so it is no longer bundled.

## Trial expiry, blocking & 30-day purge (do not "fix" or revert)

* **DEPLOYED 2026-08-12** to `brigado-a33b1` (functions → hosting → firestore:rules). Live as of that date: the trial-expiry state machine, `TrialExpiredScreen`, the `createBillingPortalSession` callable, `purgeExpiredTrials`, and — new that day — **Firestore rules enforcement** of the block. Verified in production after deploy: 21/21 client-SDK checks (blocked tenant's 8 subcollections denied read + write to an anonymous session; `la-vague` wholly unaffected), plus 50/50 emulator rules tests. Brevo shipped **inert** on this deploy — see the Brevo section.
* **States**: `restaurants/{slug}.subscriptionStatus` is the single source of truth for access blocking — `"active"` or `"trial_expired"`. Written ONLY by the Stripe webhook functions (never recomputed from dates client-side). `"trial_expired"` means the trial ended without ever converting: subscription canceled/deleted, terminal status (`canceled`/`unpaid`/`incomplete_expired`), or first payment failed for a customer who has NEVER had a paid invoice. A failed *renewal* for a previously-paying customer does NOT block (Stripe dunning handles it; a terminal status later will). `trialExpiredAt` (ISO) starts the 30-day deletion clock and is deliberately NOT reset by repeated expiry events. `suspended` is a legacy mirror kept in sync so stale client builds still block.
* **Blocking enforcement — TWO layers, both required**:
  1. *UI (App.tsx)*: gates the entire `/{slug}` app before either dashboard mounts — staff PIN pad and manager login are both unreachable when `subscriptionStatus === "trial_expired"` (renders `TrialExpiredScreen.tsx` instead). A live `onSnapshot` on the tenant doc means already-open sessions get blocked (or unblocked) in real time.
  2. *Firestore rules (added 2026-08-12)*: `tenantBlocked(restaurantId)` in `firestore.rules` reads `subscriptionStatus` off the parent tenant doc and is ANDed into every subcollection read and write, plus writes to the tenant doc. Until this existed, blocking was UI-only — anyone with the slug and an anonymous session could read/write a blocked tenant's data straight through the SDK. Do not drop this layer; the UI gate alone is not a security boundary.
* **Three things in that rules design are load-bearing — do not "tidy" them away**:
  * `.data.get('subscriptionStatus', '')` defaults a **missing** field to not-blocked. Tenants provisioned before the field existed (incl. `la-vague`, which has no `subscriptionStatus` at all) must keep working. Only the literal string `"trial_expired"` ever blocks.
  * The tenant doc itself stays **readable** while blocked — it is the doc App.tsx fetches and live-watches to discover it is blocked. Denying it replaces the explanatory block screen with a generic connection error.
  * `managers/{uid}` is **deliberately NOT gated** on subscription status: `isAuthorizedManager()` reads it during reactivation, so blocking it would break the only path an expired tenant has to pay and return. It leaks nothing (own-uid read only, `write: if false`).
* **`fetchAppData` skips subcollection reads when blocked** (`src/utils/api.ts`). This is not an optimization — it is required. `fetchAppData` `Promise.all`s all 8 subcollections, and App.tsx checks `error` *before* `trial_expired`, so a rules denial would surface as the generic "Failed to load" screen and hide the block screen (and its reactivation sign-in) behind a dead end. Keyed off the same field as the rules so the two cannot disagree.
* **Reactivation**: `TrialExpiredScreen` has its own manager sign-in (email/password + Google) which then calls the `createBillingPortalSession` callable → Stripe billing portal. That callable REQUIRES a verified manager of the restaurant — never make it unauthenticated; the portal URL exposes billing data. A successful payment (`invoice.paid` with `amount_paid > 0`), a subscription returning to `active`, or a fresh checkout with the same slug all clear the block via the webhook.
* **30-day purge**: `purgeExpiredTrials` (daily, 04:30 Paris) permanently deletes tenants with `subscriptionStatus === "trial_expired"` for 30+ days — audit record to top-level `deletionAudit` collection first (PII-light: slug, counts, timestamps), then manager lookup docs + their Auth accounts, then `recursiveDelete` of the restaurant doc and all subcollections. No Firebase Storage cleanup because the app stores nothing in Storage. **Reminder/warning emails during the 30 days are NOT sent at all right now.** They were removed from code (the old day-23 Resend final notice, deleted Aug 2026) on the assumption that a Brevo "Trial Expired" automation would replace them — **that automation was never built, and the list it would trigger from does not exist** (see the Brevo section). So the 30 days currently run silently to deletion; the block screen is the only notice, and only if the customer tries to log in.
* **Safety rail**: before ANY notice or deletion, the job re-checks live Stripe — if the customer has an `active`/`trialing`/`past_due` subscription, the flag is treated as a missed-webhook error, access is restored, and nothing is deleted. A Stripe API error skips that tenant until the next run. All transitions log loudly (reason + event id stored in `subscriptionStatusReason`) for manual investigation. **Exception (added with the admin dashboard, see below): `deletionReason === "admin_delete"` skips this Stripe re-check entirely.** A human deliberately clicking delete is not evidence of a missed webhook, so a still-live subscription must not resurrect the tenant — without this exception, an admin deleting a paying business would be silently undone by this same safety rail on the very next nightly run. The subscription itself is **not** cancelled by the admin action; that's a manual Stripe-side step, on purpose — no dashboard button should move real money.
* **GDPR reasoning**: this purge applies ONLY to tenants that never became paying customers — there is no contract and no payroll-retention duty on Brigado's side, so holding their staff PII indefinitely would itself be the exposure. 30 days' retention with emailed warnings before erasure would be a standard, defensible policy — **but note the warnings are not currently being sent** (see the purge bullet above), so as of 2026-08-12 the policy as *implemented* is 30 days' silent retention then deletion. That is a materially weaker position than the one this reasoning was written to describe, and it is the strongest practical argument for finishing the Brevo sequence. This is entirely separate from (and must not be merged with) the 5-year staff-record retention on active accounts. Known gap, deliberate: no proactive `trial_will_end` warning email (the webhook only stamps `trialWillEndNotedAt`).

## Site Manager admin dashboard (`/admin`) — Phase 1 (2026-08-13) + Phase 2 Overview (2026-08-13)

Platform-level tools for Reix, sitting outside the slug-based tenant model entirely (it is not scoped to any restaurant). Phase 1 = auth, Businesses tab, Bonus Codes tab, Admins tab. Phase 2 = the **Overview tab** (KPI cards + charts + conversion stat) — see its own section below.

* **Auth model — server-checked on every call, never trust the client.** `platformAdmins/{lowercased email}` is a Firestore collection holding `{ addedAt, addedBy }`, fully closed to client reads *and* writes (`allow read, write: if false` in rules) — the client can't even enumerate admins. Every admin Cloud Function calls `assertCallerIsPlatformAdmin(request.auth)`, which (a) requires a signed-in caller, (b) reads the email from the **verified Firebase ID token** (`request.auth.token.email`), never from `request.data` — a client can't spoof this — and (c) does a single `get()` on `platformAdmins/{email}`. The `/admin` route's own sign-in and "not authorised" screen are UX only; they decide what renders, not what's permitted. Seeded via `scripts/seed-platform-admins.mjs` (idempotent, Admin SDK, run once manually — there is deliberately no in-app way to create the *first* admin, since that would be a privilege-escalation hole). Further admins are added from the dashboard's Admins tab, itself admin-gated.
* **Reserved slug enforcement was a real, pre-existing gap**, not just a `/admin` precaution: before this, `RegisterPage.tsx` only checked whether a slug's Firestore doc existed — a restaurant could already have registered `contact`, `register`, `features`, or any other `STATIC_PAGES` key and become permanently unreachable (routed to a static page instead of the app). Fixed in both `src/utils/reservedSlugs.ts` and `functions/src/reservedSlugs.ts` (mirrored by hand — see "Stack & structure" above) and enforced server-side inside `provisionRestaurant` and `redeemBonusCode`, not just the registration form.

### Status model: active / comped / paused / trial_expired

`subscriptionStatus` now has **four** values, not two. All four share the exact same rules boundary and UI gate (`App.tsx` / `firestore.rules`'s `tenantBlocked()`) — only the *meaning* differs:

* **`active`** — unchanged: normal paying (or still-trialing) tenant, full access.
* **`comped`** *(new)* — a bonus-code signup, never touched Stripe. Behaves **exactly like `active`** for access purposes — deliberately absent from `tenantBlocked()`. Carries `compedVia` (the code), `compedGrantedAt`, `compedUntil` (ISO date, or `null` for a permanent comp). When `compedUntil` passes, the scheduled sweep (below) flips it to `trial_expired` — the comped status itself never blocks, only its expiry does.
* **`paused`** *(new)* — an admin paused this tenant. Blocks identically to `trial_expired` (same `tenantBlocked()` condition, same "nothing behind it mounts" UI gate, same Firestore rules denial on all 8 subcollections) but with **deliberately different consequences and copy**: **no deletion countdown** (stays paused indefinitely until an admin resumes or deletes it — the purge job's query is `where("subscriptionStatus", "==", "trial_expired")` only, so `paused` tenants are excluded *by construction*, not by extra exclusion logic), and **no billing-portal reactivation link** (`PausedScreen.tsx`, not `TrialExpiredScreen.tsx` — that flow is Stripe-specific and can't lift an admin pause; only "contact support" is offered, which is the honest option here). Deliberately does **not** set the legacy `suspended` flag: `markSubscriptionActive()` reactivates anything with `suspended === true`, so mirroring that flag would let a routine Stripe `invoice.paid` webhook silently un-pause an admin's decision. `pausedAt`, `pausedBy` (admin email), `pauseReason` (optional free text) are stamped; `statusBeforePause` captures what to restore on resume (so a paused *comped* tenant resumes to `comped`, not blindly to `active`) and is cleared on resume. `provisionRestaurant`'s existing-tenant branch also checks for `paused` and leaves it alone (only relinks Stripe billing) — otherwise a paused tenant re-running checkout would silently un-pause itself.
* **`trial_expired`** — unchanged in mechanics; now also the target of the two new paths below, both of which record *why* a tenant landed there without touching the pipeline's actual mechanics.

### Admin delete = existing trial-expiry pipeline, not a new deletion path — deliberate design decision

`adminDeleteBusiness` does **not** delete anything immediately. It sets `subscriptionStatus: "trial_expired"` and `trialExpiredAt: now`, exactly what a real Stripe-driven expiry does — same 30-day retention, same block screen, same eventual `purgeExpiredTrials` auto-purge. **Why reuse rather than build a separate instant-delete path**: one deletion mechanism means one retention guarantee and one audit trail to reason about, instead of two deletion code paths that could quietly drift out of sync over time. It rides two extra fields for provenance only — `deletionReason` (`"admin_delete"` vs `"comped_expired"` vs absent/`"trial_expiry"` for the ordinary case) and `deletionInitiatedBy` (admin email) — both copied into the `deletionAudit` record `purgeTenant` already writes. The one mechanical change this required: the purge job's Stripe safety-rail re-check is **skipped** when `deletionReason === "admin_delete"` (see the Safety rail bullet above) — without that, deleting a paying business from the dashboard would be silently undone by the very next nightly run, since a live subscription looks exactly like a missed webhook to that rail. **Requires typing the restaurant's name to confirm**, re-verified server-side (not just a UI gate) — this is irreversible after 30 days.

### Bonus codes (`bonusCodes/{CODE}`)

Fully closed to client access, same as `platformAdmins` (`allow read, write: if false` on the collection *and* its `redemptions` subcollection — unredeemed codes must not be readable, or anyone could list and claim them). Fields: `durationDays` (number, or `null` for a permanent comp), `maxRedemptions`, `redemptionCount`, `active`, `expiresAt` (optional), `note`, `createdAt`/`createdBy`. `redemptions/{slug}` subcollection records `{ redeemedAt, restaurantName }` per redemption.

* **`redeemBonusCode`** is the one **public, unauthenticated** callable in this whole feature — it's called from the registration form before anyone is signed in. It reuses `provisionRestaurant` (the exact function the Stripe webhook calls) with a `comped` option — **no duplicate tenant-creation logic** — skipping Stripe entirely.
* **Concurrency**: the redemption slot is **reserved inside a Firestore transaction** — incrementing `redemptionCount` and writing the `redemptions/{slug}` doc together — *before* provisioning runs. This is what makes a `maxRedemptions: 1` code safe against two simultaneous redeemers: the second transaction re-reads the already-incremented count and fails. If provisioning then throws (rare — Auth user creation, etc.), the reservation is rolled back in a second transaction so a failed signup never silently burns the last slot of a limited code. (Note: incrementing the counter *only after* provisioning succeeds, as one might first assume is cleaner, cannot actually be made concurrency-safe — reserving the slot *is* the increment. Verified with a real 3-simultaneous-redeemers-on-a-2-slot-code test against the emulator; exactly 2 succeeded, exactly 1 got `resource-exhausted`, and the final counter was exactly 2, not 3 or 1.)
* Registration form: collapsed behind a small **"J'ai un code" / "I have a code"** text link, not a field competing visually with the paid checkout button — the paid path stays primary.

### `lastActiveAt` — write-only for now, unused until Phase 2

New field on `restaurants/{slug}`, an ISO timestamp. Written by `App.tsx` on every successful app load (catches both staff PIN sessions and manager sessions through one hook, without attaching a write to every individual read/action) — **throttled to once per 30 minutes per tenant** against the previously-fetched value, so a busy dinner service costs a couple of writes, not hundreds. Skipped entirely for blocked tenants (`trial_expired`/`paused`) — the write would be denied by rules anyway, and a blocked tenant isn't "active" in any meaningful sense. **Firestore rules note**: staff run on anonymous auth and cannot pass `isManagerOf()`, so the tenant-doc `update` rule has a second, narrow branch — `isSignedIn() && affectedKeys().hasOnly(['lastActiveAt'])` — that lets any signed-in session (anonymous or manager) update *only* that one field, nothing else on the doc. **Nothing reads this field yet.** It exists purely so Phase 2's analytics view has historical data to work with instead of starting from zero when it's built.

### Phase 2 — Overview tab (2026-08-13)

Fourth tab, and the **default** one when `/admin` loads (a dashboard should open on a summary, not a list). Reuses the existing admin gate — no new access path. All of it is computed client-side from the **same `adminListBusinesses` response the Businesses tab renders**: that fetch was lifted up into `AdminDashboard` so both tabs share one query, and switching tabs no longer refetches.

* **KPI cards.** Row 1: Total businesses (all time) / Currently active (`status === "active"` **and** a Stripe customer is linked) / Currently on trial (`active` with **no** Stripe customer — also where legacy pre-Stripe tenants like `la-vague` land) / Comped. Row 2: Paused / Pending deletion (`trial_expired`, inside the 30-day window) / Active in last 24h / Active in last 7 days.
* **"Active in last 24h / 7d" is recent activity, NOT live presence.** It reads `lastActiveAt`, which is written on app load and throttled to once per 30 min per tenant. Both cards are **clickable** and drill into the Businesses tab. That required adding a fourth filter dimension (`activity`) to `BusinessesTab` alongside search/status/plan — deliberately folded into the same `useMemo` rather than built as a parallel mechanism, so all four compose. The handoff is a **one-shot** `pendingFilter` that is consumed and cleared, so the dropdowns stay manually changeable afterwards.
* **Chart 1 — signups per month, last 12 months.** Monthly (not weekly) buckets on purpose: production currently holds a single tenant from Jul 2026, so a 12-week window would render one bar in eleven empty slots. Revisit to weekly once signups-per-week is routinely > 0. Tenants with no signup date are excluded from the chart and reported beneath it with a pointer to the backfill script, rather than silently dropped or given a guessed date.
* **Chart 2 — current status breakdown**, donut: Paying / Trial / Comped / Paused / Pending deletion. Colours mirror `STATUS_STYLES` so a status reads the same here and in the Businesses list. Zero-value slices are filtered out. Uses **recharts**, already a dependency and already used by `ManagerDashboard.tsx` and `StatsPage.tsx` — no second charting library was added.
* **Conversion stat — deliberately a single current number, not a trend.** Shows `X of Y completed trials converted (Z%)`, and shows the **raw X/Y counts** rather than only a percentage precisely because the sample is currently tiny. Definition of a *completed* trial: it ended one way or the other — either **converted** (`active` + Stripe customer) or **expired** (`trial_expired`). Excluded from the denominator: comped tenants (never had a trial), tenants still inside a trial (outcome unknown), paused tenants (outcome deferred), and `trial_expired` tenants whose `deletionReason === "comped_expired"` (their comp lapsed, they never trialed). **Known caveat, documented in code:** a `trial_expired` tenant with `deletionReason === "admin_delete"` counts as "did not convert" — usually right, but an admin deleting a *paying* customer would also land there. Fine to inspect by hand at current volume.
* **Built to become a trend chart later without a rewrite.** `trialOutcomes()` returns **per-tenant dated events** (`{ slug, converted, completedAt }`), not a single aggregate — turning this into a time series is a grouping change, not a redesign. `completedAt` is exact for expiries (`trialExpiredAt`) but **derived** for conversions (signup + the 7-day trial length), because the actual Stripe conversion moment is not stored on the tenant doc. Good enough to bucket by month; do not present it as a precise conversion timestamp. **The stat is simple on purpose — it is not half-finished.**

### `createdAt` on `restaurants/{slug}` — added in Phase 2, and it did not exist before

⚠️ **Until 2026-08-13, restaurant docs stored NO creation date at all.** `provisionRestaurant` never wrote one, so `adminListBusinesses`'s `joinedAt: compedGrantedAt || createdAt || null` silently resolved to `null` for every paid signup — which is why the Businesses list showed "Joined —", and why the signups-over-time chart had no data source whatsoever. Two changes fixed this:

1. `provisionRestaurant` now writes `createdAt` (ISO) at provisioning, for all future tenants.
2. `scripts/backfill-created-at.mjs` reconstructs it for tenants that predate the field. It prefers `compedGrantedAt` (exact) and otherwise falls back to the **earliest Firebase Auth `creationTime` among that restaurant's managers** — for a webhook-provisioned tenant that is the same moment the doc was created, so it's a reconstruction rather than a guess, but it IS a proxy and is flagged as such on the doc via `createdAtBackfilled: true`. Dry-run by default, `--apply` to write, idempotent, and it **skips** (never invents a date for) any tenant with no usable source. Run against production 2026-08-13: `la-vague` → `2026-07-17T15:24:33.000Z`.

### Brevo: `SIGNUP_TYPE` attribute (added, still inert — see the Brevo section below)

`addContactToJoinedList` now writes `SIGNUP_TYPE: "paid" | "comped"` alongside the existing attributes, so a Brevo-side segment can eventually tell bonus-code signups from paying ones without inspecting `subscriptionStatus` (which changes over a tenant's life). Added to the attribute list in `scripts/brevo-setup-lists.ts`. Like every other Brevo call, this ships **inert** — see the status block below; it will start actually reaching Brevo once a valid API key exists and the setup script has run.

## Brevo (marketing/lifecycle email) — lists, attributes, trigger points

* **STATUS AS OF 2026-08-12: the whole Brevo integration is deployed but INERT.** The code is written, typechecked, deployed and guarded — but nothing reaches Brevo, by three independent causes. Do not assume any of this works; re-verify before relying on it.
  1. **The API key is revoked.** The key in the gitignored root `.env` returns `401 {"message":"Key not found"}`. Every local script (`scripts/brevo-*.ts`) fails on it. A **new key must be minted in the Brevo dashboard** and put in root `.env` before anything below can be provisioned.
  2. **`functions/.env` holds `BREVO_TRIAL_EXPIRED_LIST_ID=0` and `BREVO_JOINED_LIST_ID=0`.** `0` is falsy, so `brevoConfig()` logs "not configured" and returns null, and every list call is skipped. These are placeholders — deploy fails outright with no values at all, since `defineString` still demands them in non-interactive mode even with `default: ""`.
  3. **The Brevo account is not set up.** `Trial Expired - Brigado` **does not exist**, and 8 of the 10 contact attributes the code writes do not exist (`RESTAURANT_NAME`, `RESTAURANT_ID`, `TRIAL_EXPIRED_DATE`, `DELETION_DATE`, `REACTIVATION_URL`, `LANG`, `SUBSCRIPTION_STATUS`, `SIGNUP_TYPE`). Only `CITY`, `PHONE`, and `CONVERTED` exist.
  **To activate**: mint key → put in root `.env` → `npx tsx scripts/brevo-setup-lists.ts` (creates the list + attributes and overwrites both ids in `functions/.env`) → redeploy functions → build the automation in the Brevo UI.
* **BREVO_API_KEY — REVOKED AND DEAD AS OF 2026-08-12.** This is not "flagged for rotation"; it is non-functional right now. Brevo answers every call made with it `401 {"message":"Key not found","code":"unauthorized"}`, confirmed directly against the API this session. The key lives in Firebase Secret Manager (bound to `stripeWebhook` + `purgeExpiredTrials`) and in the gitignored root `.env` for local scripts — **both copies are dead**, so no local `scripts/brevo-*.ts` can run and no deployed function can reach Brevo. **Two Secret Manager versions exist, both ENABLED; their creation dates could not be read** (the `firebase-adminsdk` service account lacks `secretmanager.versions.list`, and `firebase functions:secrets:get` exposes only version + state; `gcloud` was unusable on this machine). Neither was destroyed. Cleanest fix when marketing resumes: mint a new key, add it as v3, then disable v1 and v2.
* **Lists** (never mix them): `Resturants - France` (id 6, cold-outreach prospects — sic, typo is in Brevo, 354 contacts), `Joined - Brigado` (id 5 — code integration now EXISTS (`addContactToJoinedList`, called from `provisionRestaurant`) but is inert per the status block above; list currently has 0 contacts), `Trial Expired - Brigado` (**not yet created** — `scripts/brevo-setup-lists.ts` creates it; note the script is `brevo-setup-lists.ts`, there is no `brevo-setup-trial-expired.ts`).
* **Trial-expired sequence — division of labour** (design intent; **none of these trigger points currently reach Brevo** — see the status block above): code (functions/src/brevo.ts) ONLY manages list membership + contact attributes; the email cadence itself (content, timing, wait/branch logic, exit conditions) is built manually in Brevo's Automation Workflow Editor — Brevo does not expose automation workflows via API, do not try. Trigger points, once activated: `markTrialExpired` → upsert contact (updateEnabled: true, no duplicates) onto the list; `markSubscriptionActive` + the re-checkout branch of `provisionRestaurant` → remove from list + `SUBSCRIPTION_STATUS="active"`; `purgeTenant` (day-30 deletion) → remove from list + `SUBSCRIPTION_STATUS="deleted"`, contact record deliberately NOT hard-deleted (consent/audit trail; re-signup just updates it).
* **Only the main registered contact** is enrolled — `signupEmail` with `managerEmails[0]` fallback for pre-Aug-2026 tenants (helper `primaryEmailOf`), never the whole manager list.
* **Contact attributes written by the code.** ⚠️ **Only 2 of the 10 listed below actually exist in Brevo today** — `CITY` and `PHONE`, which both predated this work. The other **8 have never been created**, because the setup script that creates them has never successfully run: `RESTAURANT_NAME`, `RESTAURANT_ID`, `TRIAL_EXPIRED_DATE`, `DELETION_DATE`, `REACTIVATION_URL`, `LANG`, `SUBSCRIPTION_STATUS`, `SIGNUP_TYPE`. Brevo rejects writes to undefined attributes, so even with a valid key and real list ids, the upserts would fail until the setup script runs. (`CONVERTED` — not one of the 10, written separately by `markBrevoContactConverted` — does exist, created earlier by `scripts/brevo-setup-sequence.ts`.) Verified against the Brevo attributes API 2026-08-12; `SIGNUP_TYPE` added 2026-08-13 alongside the admin dashboard's bonus-code system (see above), not yet independently re-verified against the live API — flagged as unverified rather than assumed. Full intended set: `RESTAURANT_NAME`, `RESTAURANT_ID` (slug), `CITY` (from `ownerContact.city`, "" for pre-city-field tenants), `PHONE` (from `ownerContact.phone`), `TRIAL_EXPIRED_DATE` (date), `DELETION_DATE` (date, trialExpiredAt + 30d, computed from the never-reset `trialExpiredAt` so it can't drift), `REACTIVATION_URL` (`https://brigado.solutions/{slug}` — the blocked screen with its reactivation sign-in), `LANG` (`preferredLang`, default "fr"), `SUBSCRIPTION_STATUS` (`trial_expired` / `active` / `deleted` — usable as automation exit condition alongside list removal), `SIGNUP_TYPE` (`paid` / `comped`, added with the bonus-code system).
* **Reliability rule**: every Brevo call is wrapped and logged, never thrown — Brevo being down must never block provisioning, reactivation, or the Firestore purge. Failures log `reconcile manually`. This is what makes shipping it inert safe: with the key revoked and list ids at 0, the calls skip or log and everything else carries on.
* **`CONVERTED` marker — read this before assuming it's harmless**: `markBrevoContactConverted` is gated on the API key **only**, NOT on a list id — so unlike every other Brevo call it fires on *every* provisioning as soon as a valid key exists. `CONVERTED` (boolean) does exist in Brevo, so it does not error; it writes `CONVERTED=true` to the real contact record, which for a converting prospect is one of the 354 live contacts on `Resturants - France`. It changes no email behaviour, because nothing reads the flag — the "Brigado Email Nurture Sequence #2" automation that would test it is unfinished and inactive. Also note the ordering comment in `provisionRestaurant` claims the joined-list upsert runs first so the CONVERTED `PUT` can never 404; **that invariant does not hold while the joined list is inert** (organic signups will 404, logged at info and swallowed). It becomes true again once the list ids are real.

## MARKETING — PARKED (deliberate; do not "fix" this unprompted)

**All Brevo marketing/list integration is intentionally inactive while product and site work continues.** This covers the `Joined - Brigado` list wiring, the `Trial Expired - Brigado` list wiring, `CONVERTED` tagging, and the cold-outreach nurture sequence. The **code for all of it exists, compiles, and is deployed** — it is inert because the API key is dead and the lists/attributes were never provisioned. Every call site is guarded and swallows failures, so nothing breaks as a result.

**This is a deliberate state, not a bug.** A future session finding "Brevo code that doesn't work" should NOT repair it as a drive-by fix. It stays parked until Reix says marketing is resuming.

Also parked and wired to nothing live: the **"Brigado Email Nurture Sequence #2"** automation content, and the orphaned `brigado-cold-sequence` templates (Brevo template ids 2–6, created by `scripts/brevo-setup-sequence.ts`, content in `scripts/brevo-update-templates.ts`). The trial-expired reminder cadence is likewise still unbuilt in the Brevo UI.

### Checklist for when marketing work resumes — in this order

1. **Mint a new Brevo API key.** Done manually by Reix in the Brevo dashboard, never by code or by Claude. Goes into the gitignored root `.env` (local scripts) and a new Secret Manager version (functions). Then disable the two dead versions.
2. **Add a readiness check BEFORE using the new key.** ⚠️ Do this in the same change as step 1 — do not just drop the key in. `markBrevoContactConverted` is currently gated on the API key **alone**, not on list ids, so the moment a valid key exists it starts firing `CONVERTED=true` writes against real contacts among the 354 live prospects on `Resturants - France`, with no safeguard. Gate it (and any other ungated Brevo write) behind the same "list ids actually configured" check that `brevoConfig()` already applies to everything else, so a key alone can never start mutating the prospect database.
3. **Run the setup script** to create the real list + attributes: `npx tsx scripts/brevo-setup-lists.ts`. It creates `Trial Expired - Brigado`, resolves `Joined - Brigado` (id 5, already exists), creates the 7 missing attributes, and overwrites `BREVO_TRIAL_EXPIRED_LIST_ID` / `BREVO_JOINED_LIST_ID` in `functions/.env` with the real ids. Redeploy functions afterwards so the new `defineString` values are picked up. *(Historical note: earlier versions of this file called this script `scripts/brevo-setup-trial-expired.ts`. No such file has ever existed — `brevo-setup-lists.ts` is the real one and does both lists.)*
4. **Build the actual email content and timing in Brevo's UI.** Content, delays, branching and exit conditions are all manual — Brevo does not expose automation workflows via API, per the standing repo convention on automation branching. Do not attempt to script this.

Verify each step against the Brevo dashboard/API before marking it done — see the documentation-hygiene rule in "Working style".

## Known pending work / open items

* **Admin dashboard Phase 2 Overview tab is BUILT** (2026-08-13) — KPI cards, signups chart, status donut, conversion stat. See its section above. Still open for a future phase: a conversion **trend over time** (the data layer is already shaped for it, see `trialOutcomes()`), churn/retention, and per-tenant engagement depth. `lastActiveAt` now has a real reader.
* **Overview KPIs are honest but nearly empty right now, by data not by bug.** Production holds exactly **one** restaurant (`la-vague`), which is a legacy pre-Stripe tenant: no `subscriptionStatus`, no `stripeCustomerId`. So it reads as Total 1 / Paying 0 / Trial 1, conversion "No completed trials yet", one chart bar in Jul 2026, one donut slice. That is all correct. Don't "fix" it — verified against a direct Firestore count 2026-08-13.
* **`SIGNUP_TYPE` Brevo attribute is unverified.** Added to the code and to `scripts/brevo-setup-lists.ts` alongside the bonus-code system, but — like the rest of Brevo — not independently re-confirmed against the live attributes API per the documentation-hygiene rule below. Verify it (and everything else in the Brevo section) before relying on it.
* **ACCEPTED GAP (2026-08-12): an expiring tenant gets NO proactive email.** The old day-23 Resend final notice was removed, and the Brevo replacement is unbuilt and inert. So the `TrialExpiredScreen` block screen is currently the **only** channel telling a customer their trial ended, that data is kept 30 days, the exact deletion date, and how to reactivate — and they only see it if they try to log in. Verified accurate and complete on production 2026-08-12 (cause, 30-day retention, correct `trialExpiredAt`+30d date, reactivation sign-in, support address). Minor: if `trialExpiredAt` were ever absent, the date line silently omits. This is a known, accepted trade-off — not a bug to "fix" in passing, but the reason the Brevo sequence matters.
* **Brevo activation** is the main outstanding follow-up — see the three blockers and the activation recipe in the Brevo section above. Needs a freshly minted API key first.
* ~~**StatsPage.tsx integration**~~ — **RESOLVED / was already done.** Verified 2026-08-13: `src/components/StatsPage.tsx` exists and **is** wired into `ManagerDashboard.tsx` (imported line 19, rendered line 3693). This entry was stale. It is also the precedent for charting in this repo: **recharts**, already a dependency — reuse it rather than adding a second charting library.
* This repo was not being pushed to GitHub during earlier development (all changes were applied locally from downloaded files). Recommend setting up a proper `git add / commit / push` habit now that Claude Code has direct file access, so this doesn't happen again — the working directory should become the single source of truth living in both places.
* Custom-roles-vs-fixed-list decision: currently a fixed but generous role list (server/kitchen/cold/dishwasher/bar/chef/cleaner/host/other). Full custom roles per restaurant would be a bigger schema change — only worth it if the fixed list proves genuinely limiting.
* Paid-leave accrual tracking (5-week entitlement, balance tracking) is explicitly NOT built — flagged as a bigger future feature, not a bug.
* GDPR "right to erasure" beyond the 5-year staff-deletion gate is not implemented — deliberately deferred pending the user consulting an actual advisor, since erasure requests can legally conflict with payroll retention requirements.

## Secrets / external services

* Firebase Secret Manager holds `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `RESEND_API_KEY`, `BREVO_API_KEY` — never hardcode these, never put them in client-side code (functions only). **`BREVO_API_KEY` is currently revoked/dead** — see the Brevo section for details and the replacement procedure.
* Resend sending domain: `brigado.solutions`, verified. Sender address convention: `info@brigado.solutions`.
* Any API key or credential that appears in chat/conversation history should be treated as compromised and rotated — this has happened before with both the Firebase service account key and the Resend key.

## Working style established with this project

* Always run `npx tsc --noEmit` and a full `npm run build` (both root app and `functions/` separately) before considering a change done. **`npm test` (vitest) is the test runner** — added 2026-08-15 with the scheduling/payroll series; it currently covers `src/utils/effectiveHours.ts` only, so it is a fast check, not a safety net for the rest of the app. `lint` is still just `tsc --noEmit`.
* Deploys: `firebase deploy --only hosting` for app changes, `firebase deploy --only functions` for Cloud Functions changes (both if both changed). `firebase use brigado-a33b1` to confirm the right project before deploying (there was a past incident of deploying to the wrong Firebase project). The bundled Firebase skills (`.agents/skills/firebase-basics`) say to invoke the CLI as `npx -y firebase-tools@latest` rather than bare `npx firebase-tools`, so the version is pinned to latest rather than whatever happens to be cached.
* **Functions deploys are flaky near the module-discovery timeout — use `FUNCTIONS_DISCOVERY_TIMEOUT`.** `firebase deploy --only functions` loads `functions/lib/index.js` to enumerate exports, and aborts with *"User code failed to load. Cannot determine backend specification. Timeout after 10000"* if that takes over 10s. **Measured 2026-08-15: the module takes ~8.8s to load on this machine** (firebase-admin + stripe + 25 exports), i.e. only ~1.2s of headroom — which is why the same codebase deploys fine most of the time and then randomly doesn't. It is NOT a CLI-version regression and not caused by any particular code change. Fix: prefix the deploy with `FUNCTIONS_DISCOVERY_TIMEOUT=180`. If module load ever grows past ~10s outright, the real fix is deferring heavy top-level imports, not raising the timeout further.
* The user (Reix) runs a real restaurant (La Vague, slug `la-vague`) as the pilot customer — treat that restaurant's data/settings as real production data, not a test fixture.
* **Documentation hygiene — verify before writing "done" in this file.** Only record something as done, existing, or working after confirming it **from the authoritative source**: for an external service (Brevo, Stripe, Resend, Firebase console) that means its own dashboard or API, not the code. Code compiling, a function deploying, or a call returning 200 proves none of these things — a guarded call that logs and skips also "succeeds". This rule exists because the 2026-08-12 session found **three separate false "done" claims** in this file that ordinary code review had not caught: a Brevo list that was never created, a setup script that never existed under the name given, and a set of contact attributes that were documented as "all exist" when 7 of 9 did not. Each looked correct in the source. When you cannot verify something, write down what you could not verify and why — an explicit unknown is useful, a confident wrong claim is worse than silence.
* Prefer testing security rules against the **Firestore emulator** (`firebase emulators:exec --only firestore`) rather than reasoning about them. Rules changes are hard to reason about and easy to get subtly wrong — the trial-expiry rules landed with 50 emulator assertions covering active / legacy-missing-field / blocked tenants plus cross-tenant and unauthenticated access, then 21 more against production after deploy. Java is required for the emulator and is installed on this machine; port 8080 is occupied, so pass a config with a free port.
